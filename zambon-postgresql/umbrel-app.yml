manifestVersion: 1
id: zambon-postgresql
name: PostgreSQL
tagline: Powerful open-source relational database system
icon: https://img.icons8.com/color/480/postgreesql.png
category: Development
version: "17.9"
port: 5432
description: >-
  PostgreSQL is the world's most advanced open-source relational database.
  Run it directly on your Umbrel server to host databases for your applications,
  perform data analytics, and manage structured data with full SQL support.


  Connects on port 5432 with the default credentials configured in the container.
  Use any PostgreSQL client (pgAdmin, DBeaver, psql) to connect and manage your databases.

developer: PostgreSQL Global Development Group
website: https://www.postgresql.org
submitter: Ricardo Zambon
submission: https://github.com/RicardoZambon
repo: https://github.com/docker-library/postgres
support: https://github.com/docker-library/postgres/issues
gallery:
  - https://i.imgur.com/yyVG0Jb.jpeg
releaseNotes: >-
  Applies non-default server tuning, and tracks the patch version the
  postgres:17-alpine image actually ships.


  idle_in_transaction_session_timeout was 0, meaning an idle-in-transaction
  session is never reaped. A client that keeps a background reader open then
  blocks DDL and TRUNCATE indefinitely, and the symptom is a job that hangs
  rather than an error. It is now 5 minutes.


  max_connections goes from 100 to 200, which the default leaves thin once
  several applications share one instance and each opens its own pool.
  shared_buffers, work_mem and the WAL sizes are raised to match a server with
  room to spare; scale them to your own hardware if yours is smaller.


  Your data is untouched by this update.
dependencies: []
path: ""
defaultUsername: "postgres"
defaultPassword: "Postgres2025!"
